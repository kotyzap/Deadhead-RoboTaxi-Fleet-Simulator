/* Real admin credentials — gitignored, never committed (see .gitignore and
   admin-config.example.js). Edit these two values and redeploy to change
   either one. */
export const ADMIN_EMAIL = 'kotyza@gmail.com';
export const ADMIN_PASSWORD = 'LTMX4CU9UHyYEMvRDhpx';
